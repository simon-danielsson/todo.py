[project]
name = "todo-py"
version = "0.1.3"
description = "A search utility for TODO statements in codebases."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
license = { text = "MIT" }
authors = [{ name = "Simon Danielsson" }]

[project.scripts]
todo = "todo.cli:main"
